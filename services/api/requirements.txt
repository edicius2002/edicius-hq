fastapi==0.141.1
# `UploadFile` for the portable watched-route import is parsed as multipart.
python-multipart==0.0.32
uvicorn[standard]==0.52.4
httpx==0.28.1
supabase==2.31.0
PyJWT[crypto]==2.14.0
playwright==1.63.0
# Arrives with uvicorn[standard], but the streaming adapters import it directly
# — a transitive dependency you rely on by name is one an upgrade can remove.
websockets==15.0.1
# Windows has no system zone database, so `ZoneInfo("America/New_York")` fails without it.
tzdata==2026.4
pytest==9.1.1
# Lint and format for the API. Pinned like everything else: the code already
# carried `# noqa` codes for a linter nobody had installed, which suppressed
# nothing and told no one.
ruff==0.16.6
mypy==2.3.1
